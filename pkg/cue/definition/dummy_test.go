// Package definition provides utilities for working with definition resources
package definition

import (
	"testing"
)

// TestDummy is a simple test to verify the environment setup
func TestDummy(t *testing.T) {
	// This test does nothing, but ensures the package can be compiled
}
